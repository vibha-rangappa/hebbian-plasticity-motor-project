"""
This package holds everything for the plasticity training experiments: the
center-out reaching task, the STDP-enabled network (built on top of the
circuit baseline), the training loop, and the HDF5 snapshot read/write code
used to save and load training results.
"""
